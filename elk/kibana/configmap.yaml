apiVersion: v1
kind: ConfigMap
metadata:
  name: kibana
  namespace: elk
data:
  kibana.yml: |
    #
    # ** THIS IS AN AUTO-GENERATED FILE BY KUBERNETES CONFIGMAP **
    #
    
    # Default Kibana configuration for docker target
    server.name: kibana
    server.host: "0"
    elasticsearch.hosts: [ "http://elasticsearch-discovery:9200" ]
    xpack.monitoring.ui.container.elasticsearch.enabled: true
    # Chinese UI
    i18n.locale: "zh-CN"
    # elastalert
    # elastalert-kibana-plugin.serverHost: elastalert
    # elastalert-kibana-plugin.serverPort: 3030
